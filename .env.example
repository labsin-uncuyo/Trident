# Core lab settings
RUN_ID=                 # optional; leave empty to auto-generate per `make up`
LAB_PASSWORD=adminadmin
COMPOSE_PROJECT_NAME=lab

# Defender / planner
DEFENDER_PORT=8000
PLANNER_PORT=1654
PLANNER_URL=http://127.0.0.1:1654/plan
OPENCODE_TIMEOUT=450

# LLM/OpenAI access (used by compromised/defender, optional for Aracne)
OPENAI_API_KEY=
OPENAI_BASE_URL=
LLM_MODEL=gpt-4o-mini

# OpenCode (server/compromised, and defender auto_responder)
OPENCODE_API_KEY=your-opencode-api-key

# Web login app (server)
LOGIN_USER=admin
LOGIN_PASSWORD=admin

# PostgreSQL (server)
DB_USER=labuser
DB_PASSWORD=labpass
